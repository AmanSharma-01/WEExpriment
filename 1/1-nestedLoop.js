// Using javascript demonstrate Nested loop.

arr = [['hi', 'this' , 'is', 'a', '2D', 'array'], ['C', 'D', 'E', 'F']]

for (i = 0; i < arr.length; i++) {
    for (j = 0; j < arr[i].length; j++) {
        console.log(arr[i][j]);
    }
}